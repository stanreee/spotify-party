{
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "start": "cd backend && node server.js",
        "heroku-postbuild": "cd frontend/spotify-party && npm install && npm run build && cd .. && cd .. && cd backend && npm install"
    }
}